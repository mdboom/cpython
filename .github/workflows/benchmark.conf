[config]
json_dir = {PYTHON_BENCH_JSON}

[scm]
repo_dir = {GITHUB_WORKSPACE}
update = False

[compile]
bench_dir = {PYTHON_BENCH_BUILD}

[compile_all]
branches = {PYTHON_BENCH_REF} {PYTHON_BENCH_BASE_REF} {PYTHON_BENCH_REF} {PYTHON_BENCH_BASE_REF}